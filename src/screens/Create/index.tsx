import React, { View, SafeAreaView, Text } from "react-native";

const CreateScreen = () => {
  return (
    <SafeAreaView>
      <Text>新建</Text>
    </SafeAreaView>
  );
};

export default CreateScreen;
